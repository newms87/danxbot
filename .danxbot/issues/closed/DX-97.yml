schema_version: 5
tracker: trello
id: DX-97
external_id: 69f77e9cbe3dbb53b4f10349
parent_id: null
children: []
dispatch:
  id: 4056323b-243b-4b9c-ab58-91c0b83ef695
  pid: 0
  host: ""
  kind: work
  started_at: ""
  ttl_seconds: 0
status: Done
type: Feature
title: Fix dispatch-pipeline.test.ts 'extracts summary from last assistant text in JSONL' race. Test reads the assistant summary before fake-claude has finished writing the final text block, so it captures the intermediate 'I will help you with that task.' instead of 'Task completed successfully'. Likely needs a wait-for-final-block helper or to drive the assertion off the watcher's terminal entry rather than the in-flight one.
description: ""
priority: 3
triage:
  expires_at: ""
  reassess_hint: ""
  last_status: ""
  last_explain: ""
  ice:
    total: 0
    i: 0
    c: 0
    e: 0
  history: []
ac: []
comments:
  - id: 69f81a8bffd27154b0a9d4d8
    author: danxbot
    timestamp: 2026-05-04T04:03:23.722Z
    text: |-
      <!-- danxbot -->
      <!-- danxbot-retro -->

      ## Retro

      **What went well:** Race reproduced deterministically (writeDelayMs=2500 + lingerMs=100 vs the watcher's 5s pollIntervalMs) before any code change, then ran 10x under load to confirm the fix is stable. Quality gates flagged a real defensive gap (drain rejection would strand the job inside an async Node 'close' listener) and that fix shipped in the same commit. Single Fork Principle preserved — no second spawn, no second observer; just one extra `drain()` call on the existing watcher.

      **What went wrong:** First fix iteration introduced 24 unit-test failures because the close handler signature changed sync → async. Each test had to await a microtask after `child.emit("close", N)`. The mock-watcher call-order assertion in launcher.test.ts also needed an update (drain runs once in close handler + once in cleanup; both are needed for different invariants — summary capture and dispatchTracker totals). Caught and fixed before commit.

      **Action items:** Nothing
      **Commits:** 9bf2e50
history: []
retro:
  good: |
    Race reproduced deterministically (writeDelayMs=2500 + lingerMs=100 vs the watcher's 5s pollIntervalMs) before any code change, then ran 10x under load to confirm the fix is stable. Quality gates flagged a real defensive gap (drain rejection would strand the job inside an async Node 'close' listener) and that fix shipped in the same commit. Single Fork Principle preserved — no second spawn, no second observer; just one extra `drain()` call on the existing watcher.
  bad: |
    First fix iteration introduced 24 unit-test failures because the close handler signature changed sync → async. Each test had to await a microtask after `child.emit("close", N)`. The mock-watcher call-order assertion in launcher.test.ts also needed an update (drain runs once in close handler + once in cleanup; both are needed for different invariants — summary capture and dispatchTracker totals). Caught and fixed before commit.
  action_item_ids: []
  commits:
    - 9bf2e50
waiting_on: null
blocked: null
