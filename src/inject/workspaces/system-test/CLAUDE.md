# system-test workspace

Dispatched cwd for `make test-system-*`. No external MCP servers — the
danxbot infrastructure server (with `danxbot_complete`) is merged in by
the dispatch core, and claude built-ins are all available by default.
